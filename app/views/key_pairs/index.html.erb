<% title _("SSH keys for: %s") % @compute_resource.name %>
<%= react_component('KeyPairsIndex', {
  apiUrl: compute_resource_key_pairs_path(@compute_resource, format: :json),
  controller: 'key_pairs',
  searchable: false,
  creatable: false,
  computeResourceId: @compute_resource.id,
  computeResourceName: @compute_resource.name,
}) %>
