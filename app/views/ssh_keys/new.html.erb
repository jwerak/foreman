<%= react_component('FormPage', react_form_props(@ssh_key, controller: 'ssh_keys', cancel_url: edit_user_path(@user)).merge(
  apiUrl: "/api/v2/users/#{@user.id}/ssh_keys",
  fields: @form_fields
)) %>
